증상: 결과가 전부 []. 개수 6 / 20 / 2 / 15 (정답 6 / 10 / 3 / 1)

버그 1 — result.append(current_combination) (뿌리 A, 02번 재발)

복사본이 아니라 원본 주소를 넣음 → result 안의 모든 칸이 같은 리스트를 가리킴. 탐색이 끝나면 pop() 이 전부 실행돼 그 리스트가 비고, 결과가 전부 []. 145~146행 주석이 이 함정을 미리 경고하고 있었는데 그대로 밟았다.

버그 2 — range(start, n) (뿌리 C)

range(a,b) 는 b 를 포함하지 않음 → 숫자 n 이 후보에서 통째로 누락. n+1 이어야 함.

버그 3 — backtrack(num, ...) (뿌리 C)

num+1 이어야 하는데 num. 한 글자 차이로 문제가 바뀐다. 같은 숫자를 다시 고를 수 있게 되어 조합(combination)이 아니라 중복조합이 됨.