| C 에서는 | 파이썬은 | 내가 쓴 잘못된 코드 | 만난 곳 |
|---|---|---|---|
| `for(int i=..)` 는 루프 스코프를 만든다 | 루프 변수가 바깥 변수를 덮어쓴다 | `for n in range(0,n)` | w2-02 |
| 루프 안 `i++` 는 실제로 두 칸 뛴다 | 다음 회차에 덮어써져 무효 | `x = x + 1` | w2-02 |
| 배열은 인덱스로 돌고 `arr[i]` 로 꺼낸다 | `for x in arr` 의 x 가 이미 원소 | `students[x]` | w2-03 |
| 구조체 대입은 값 복사 | 컨테이너 삽입은 주소 복사 | `result.append(원본)` | w2-02, w2-06 |
| | | | |
