````
queue = deque([0] * vertices)
i = 0
for index, data in enumerate(in_degree):
    if data == 0:
        queue[i] = edges[index]
        i += 1  # 큐에 추가

result = []

# TODO: 큐가 빌 때까지 반복
## 큐에서 정점 꺼내기
## 인접한 정점들의 진입 차수 감소
for data in queue:
    if data != 0:
        queue.popleft()

````

## 실수

C처럼 사용하는 배열

멍청아